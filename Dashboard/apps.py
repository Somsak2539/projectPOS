from django.apps import AppConfig




class DashboardConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'Dashboard'
    verbose_name = "1 รายการที่บันทึกสำหรับการขาย"  # เปลี่ยนเป็นชื่อที่ต้องการ
